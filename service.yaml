# NodePort 30888 is what the k3d cluster maps to localhost:8888
apiVersion: v1
kind: Service
metadata:
  name: playground
spec:
  type: NodePort
  selector:
    app: playground
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30888
